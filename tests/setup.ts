// const enum Dirv {
//   If = '#if',
//   // Else = '#else',
//   // Elif = '#elif',
//   Endif = '#endif',
// }

Reflect.set(globalThis, 'Dirv', {
  If: '#if',
  // Else : '#else',
  // Elif : '#elif',
  Endif: '#endif',
});
